scratch/
*.py.bak
.venv/
__pycache__/
*.pyc
.DS_Store
data/_card_test/
data/agent-loop/extra.jsonl
adapters/
fused/
gguf/
*.gguf
*.safetensors
.mlx_cache/
wandb/
.env
.env.*
!.env.example
hf_token
*.pem
.coverage

# Turns recorded by a run, for measuring it later.
.python-vibe/

# Turns gathered by scripts/weights/collect_traces.py.
data/agent-loop/collected.jsonl

# Editor drop-ins the harness writes into a project it is pointed at.
# Generated by `harness editors`, and tasks.json holds the absolute path
# of whoever generated it, so it is theirs and not the repository's.
demo/orders/.vscode/
demo/orders/.cursor/
